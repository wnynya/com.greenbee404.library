{
  "name": "greenbee404",
  "version": "1.0.0",
  "description": "greenbee 404 Library",
  "main": "server.js",
  "author": "Greenbee",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch ./ --ignore ./public/ --ignore ./data/ --ignore ./resource/ --ignore ./resource-babel/",
    "babel": "npx babel resource-babel --watch --out-dir resource"
  },
  "dependencies": {
    "express": "^4.17.1",
    "pug": "^3.0.2",
    "spdy": "^4.0.2",
    "vhost": "^3.0.2"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.14.8",
    "@babel/core": "^7.14.8",
    "@babel/preset-env": "^7.14.9"
  }
}
